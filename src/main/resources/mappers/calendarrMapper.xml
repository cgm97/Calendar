<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="calendarMapper">
   <!-- 일정 목록 - 아이디 별 검색 -->
   <select id="getCalendar" parameterType="String" resultType="com.kyumin.calendar.domain.CalendarDTO">
   			SELECT * FROM CALENDAR WHERE LOGINID=#{loginId}
   </select>
</mapper>